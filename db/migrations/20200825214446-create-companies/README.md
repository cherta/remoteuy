# Migration `20200825214446-create-companies`

This migration has been generated by Gabriel Chertok at 8/25/2020, 6:44:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Company" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "logo" TEXT NOT NULL,
    "hasOffices" BOOLEAN NOT NULL,
    "allowsFullRemote" BOOLEAN NOT NULL,
    "userId" INTEGER NOT NULL,

FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "Company_userId_unique" ON "Company"("userId")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200825212132-initial-migration..20200825214446-create-companies
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = ["sqlite", "postgres"]
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -20,8 +20,9 @@
   email          String    @unique
   hashedPassword String?
   role           String    @default("user")
   sessions       Session[]
+  company        Company?
 }
 model Session {
   id                 Int       @default(autoincrement()) @id
@@ -35,4 +36,17 @@
   antiCSRFToken      String?
   publicData         String?
   privateData        String?
 }
+
+model Company {
+  id               Int      @default(autoincrement()) @id
+  createdAt        DateTime @default(now())
+  updatedAt        DateTime @updatedAt
+  name             String
+  description      String
+  logo             String
+  hasOffices       Boolean
+  allowsFullRemote Boolean
+  user             User     @relation(fields: [userId], references: [id])
+  userId           Int
+}
```


